parameters:
    gk_mailer.class:      Site\CommonBundle\Mailer
    gk_mailer.transport:  sendmail

services:
    gk_mailer:
        class:        "%gk_mailer.class%"
        arguments:    ["%gk_mailer.transport%"]
